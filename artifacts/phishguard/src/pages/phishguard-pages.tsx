import { useState } from 'react';
import { useParams, Link } from 'wouter';
import { useQueryClient } from '@tanstack/react-query';
import { useUser, useClerk } from '@clerk/react';
import {
  ArrowLeft,
  ArrowRight,
  BookOpen,
  Check,
  FileCheck2,
  Flag,
  LockKeyhole,
  LogOut,
  Pencil,
  Plus,
  Radar,
  Save,
  ShieldAlert,
  ShieldCheck,
  Sparkles,
  Target,
  Trash2,
  Users,
} from 'lucide-react';
import {
  Campaign,
  QuizQuestion,
  getGetAdminStatsQueryKey,
  getGetCampaignQueryKey,
  getGetDashboardQueryKey,
  getListCampaignsQueryKey,
  getListQuizQuestionsQueryKey,
  getListRecommendationsQueryKey,
  useCreateCampaign,
  useCreateQuizQuestion,
  useDeleteCampaign,
  useDeleteQuizQuestion,
  useGetAdminStats,
  useGetCampaign,
  useGetDashboard,
  useListCampaigns,
  useListQuizQuestions,
  useListRecommendations,
  useSubmitCampaignAttempt,
  useSubmitQuiz,
  useUpdateCampaign,
  useUpdateQuizQuestion,
} from '@workspace/api-client-react';
import { AppShell, Button, EmptyState, ErrorState, Field, LoadingState, PageHeader, Pill, ProgressBar, SectionLabel, StatCard, TextAreaField, cx } from '@/components/phishguard-ui';

const difficultyLabels: Record<string, string> = { beginner: 'débutant', intermediate: 'intermédiaire', advanced: 'avancé' };
const answerLabels: Record<string, string> = { phishing: 'hameçonnage', legitimate: 'légitime' };
const priorityLabels: Record<string, string> = { high: 'priorité haute', medium: 'priorité moyenne', low: 'priorité basse' };

export function HomePage() {
  return <div className="min-h-[100dvh] bg-background text-foreground"><nav className="mx-auto flex max-w-7xl items-center justify-between px-5 py-6 sm:px-8"><div className="flex items-center gap-3"><img src="/logo.svg" alt="PhishGuard" className="h-10 w-10" /><span className="text-lg font-extrabold tracking-[-.05em]">Phish<span className="text-primary">Guard</span></span></div><div className="flex items-center gap-3"><Link href="/sign-in" className="rounded-lg px-3 py-2 text-sm font-bold text-muted-foreground hover:text-foreground" data-testid="link-sign-in">Se connecter</Link><Link href="/sign-up" className="rounded-[10px] bg-primary px-4 py-2.5 text-sm font-bold text-primary-foreground shadow-[0_4px_0_hsl(var(--primary)/.18)] hover:-translate-y-0.5" data-testid="link-sign-up">Commencer</Link></div></nav><main><section className="security-grid relative overflow-hidden border-y border-border/70"><div className="mx-auto grid max-w-7xl items-center gap-12 px-5 py-20 sm:px-8 lg:grid-cols-[1.05fr_.95fr] lg:py-28"><div className="animate-rise"><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-primary/20 bg-primary/5 px-3 py-1.5 font-mono text-[10px] uppercase tracking-[.15em] text-primary"><span className="h-1.5 w-1.5 rounded-full bg-primary" /> Une façon plus sereine d’apprendre la sécurité</div><h1 className="max-w-3xl text-5xl font-extrabold leading-[.98] tracking-[-.075em] sm:text-7xl">Faites confiance à vos <span className="text-primary">réflexes.</span><br />Vérifiez les détails.</h1><p className="mt-7 max-w-xl text-base leading-7 text-muted-foreground sm:text-lg">PhishGuard est un espace privé pour apprendre à repérer les messages suspects, adopter les bons réflexes et progresser avec des scénarios sûrs.</p><div className="mt-9 flex flex-wrap gap-3"><Link href="/sign-up" className="inline-flex h-12 items-center gap-2 rounded-xl bg-primary px-5 text-sm font-bold text-primary-foreground shadow-[0_6px_0_hsl(var(--primary)/.18)] transition hover:-translate-y-0.5" data-testid="link-hero-start">Accéder à l’espace d’entraînement <ArrowRight size={17} /></Link><a href="#how-it-works" className="inline-flex h-12 items-center rounded-xl border border-border bg-card px-5 text-sm font-bold hover:border-primary/40" data-testid="link-hero-learn">Voir comment ça marche</a></div><div className="mt-10 flex items-center gap-7 font-mono text-[10px] uppercase tracking-[.12em] text-muted-foreground"><span className="flex items-center gap-2"><ShieldCheck size={15} className="text-primary" /> scénarios fictifs</span><span className="flex items-center gap-2"><LockKeyhole size={15} className="text-primary" /> progression privée</span></div></div><div className="scan-line animate-rise delay-2 relative rounded-[28px] border border-border bg-card p-5 shadow-[0_24px_70px_hsl(var(--foreground)/.1)]"><div className="flex items-center justify-between border-b border-border pb-4"><div><SectionLabel>aperçu du scénario / 001</SectionLabel><p className="mt-1 text-sm font-bold">Un message à regarder de plus près</p></div><Pill tone="amber">entraînement</Pill></div><div className="mt-5 rounded-xl border border-border bg-background p-4"><div className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-full bg-secondary text-xs font-extrabold text-secondary-foreground">IT</div><div><p className="text-xs font-bold">Service informatique du campus</p><p className="font-mono text-[10px] text-muted-foreground">assistance@campus-acces.co</p></div><span className="ml-auto font-mono text-[10px] text-muted-foreground">09:42</span></div><p className="mt-5 text-sm font-bold">Votre compte nécessite votre attention</p><p className="mt-2 text-xs leading-5 text-muted-foreground">Nous avons détecté une connexion depuis un nouvel emplacement. Confirmez vos informations pour conserver l’accès à vos outils de cours.</p><div className="mt-4 rounded-lg border border-accent/40 bg-accent/10 px-3 py-2 font-mono text-[10px] text-amber-900">https://campus-acces.co/verifier</div></div><div className="mt-4 grid grid-cols-2 gap-3"><div className="rounded-xl bg-primary/5 p-3"><p className="font-mono text-[10px] uppercase text-primary">Votre mission</p><p className="mt-1 text-xs font-bold">Légitime ou hameçonnage&nbsp;?</p></div><div className="rounded-xl bg-muted p-3"><p className="font-mono text-[10px] uppercase text-muted-foreground">Aucun risque</p><p className="mt-1 text-xs font-bold">Se tromper est sans danger</p></div></div></div></div></section><section id="how-it-works" className="mx-auto max-w-7xl px-5 py-20 sm:px-8"><div className="max-w-xl"><SectionLabel>Le cycle d’entraînement</SectionLabel><h2 className="mt-3 text-3xl font-extrabold tracking-[-.06em] sm:text-4xl">Observez. Décidez. Retenez.</h2><p className="mt-4 text-sm leading-6 text-muted-foreground">De courts exercices vous aident à reconnaître les signaux quand cela compte. Pas de peur, pas de vrais liens, pas de piège en dehors de la leçon.</p></div><div className="mt-12 grid gap-4 md:grid-cols-3"><div className="rounded-2xl border border-border bg-card p-6"><span className="font-mono text-sm text-primary">01</span><BookOpen className="mt-10 text-primary" size={22} /><h3 className="mt-5 font-bold">Lisez le signal</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">Explorez des messages fictifs construits autour des détails souvent utilisés&nbsp;: urgence, autorité et destinations inhabituelles.</p></div><div className="rounded-2xl border border-border bg-card p-6"><span className="font-mono text-sm text-primary">02</span><Radar className="mt-10 text-primary" size={22} /><h3 className="mt-5 font-bold">Faites votre choix</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">Choisissez une réponse, signalez les indices repérés et recevez tout de suite une explication simple.</p></div><div className="rounded-2xl border border-border bg-card p-6"><span className="font-mono text-sm text-primary">03</span><FileCheck2 className="mt-10 text-primary" size={22} /><h3 className="mt-5 font-bold">Créez le réflexe</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">Utilisez vos retours, vos conseils et votre historique pour bien cibler les dix prochaines minutes.</p></div></div></section><section className="bg-sidebar text-sidebar-foreground"><div className="mx-auto flex max-w-7xl flex-col gap-8 px-5 py-14 sm:px-8 md:flex-row md:items-center md:justify-between"><div><SectionLabel><span className="text-sidebar-primary">Pour les communautés universitaires</span></SectionLabel><h2 className="mt-3 max-w-2xl text-3xl font-extrabold tracking-[-.06em]">La confiance est un contrôle de sécurité.</h2></div><Link href="/sign-up" className="inline-flex h-11 items-center gap-2 self-start rounded-xl bg-sidebar-primary px-5 text-sm font-bold text-sidebar-primary-foreground hover:brightness-105" data-testid="link-footer-start">Construire ses réflexes <ArrowRight size={16} /></Link></div></section></main><footer className="mx-auto flex max-w-7xl items-center justify-between px-5 py-7 text-xs text-muted-foreground sm:px-8"><span>PhishGuard · espace universitaire d’apprentissage</span><span className="font-mono">fictif / privé / pratique</span></footer></div>;
}

function DashboardPage() {
  const query = useGetDashboard();
  if (query.isLoading) return <AppShell><LoadingState /></AppShell>;
  if (query.isError || !query.data) return <AppShell><ErrorState onRetry={() => query.refetch()} /></AppShell>;
  const dashboard = query.data;
  const score = dashboard.latestScore ?? 0;
  return <AppShell><PageHeader eyebrow="Vue d’ensemble" title="Restez vigilant, sereinement." description="Un aperçu rapide de votre entraînement. De petites sessions régulières font la différence." action={<Link href="/campaigns" className="inline-flex min-h-10 items-center gap-2 rounded-[10px] bg-primary px-4 text-sm font-bold text-primary-foreground" data-testid="link-dashboard-action">S’entraîner avec un scénario <ArrowRight size={16} /></Link>} /><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Scénarios terminés" value={dashboard.campaignsCompleted} detail="scénarios fictifs analysés" icon={Radar} /><StatCard label="Quiz réalisés" value={dashboard.quizzesCompleted} detail="réponses mémorisées" icon={FileCheck2} tone="amber" /><StatCard label="Dernier score" value={`${score}%`} detail="sur votre dernier quiz" icon={Target} /><StatCard label="Niveau actuel" value={dashboard.level || 'À découvrir'} detail={`${dashboard.streak} jour${dashboard.streak > 1 ? 's' : ''} d’affilée`} icon={Sparkles} tone="slate" /></div><div className="mt-7 grid gap-6 lg:grid-cols-[1.25fr_.75fr]"><section className="rounded-2xl border border-border bg-card p-6 sm:p-8"><div className="flex items-start justify-between gap-4"><div><SectionLabel>Prochaine étape</SectionLabel><h2 className="mt-2 text-2xl font-extrabold tracking-[-.05em]" data-testid="text-next-action">{dashboard.nextAction || 'Découvrir un nouveau scénario'}</h2><p className="mt-3 max-w-lg text-sm leading-6 text-muted-foreground">Gardez la session courte et précise. Une révision ciblée vaut mieux qu’un long défilement.</p></div><div className="hidden h-12 w-12 items-center justify-center rounded-2xl bg-primary/10 text-primary sm:flex"><Flag size={21} /></div></div><div className="mt-8 flex flex-wrap gap-3"><Link href="/quiz" className="inline-flex h-10 items-center gap-2 rounded-lg bg-foreground px-4 text-sm font-bold text-background" data-testid="link-next-quiz">Faire un quiz rapide <ArrowRight size={15} /></Link><Link href="/recommendations" className="inline-flex h-10 items-center gap-2 rounded-lg border border-border px-4 text-sm font-bold hover:border-primary/40" data-testid="link-next-guidance">Voir mes conseils</Link></div></section><section className="rounded-2xl bg-secondary p-6 sm:p-8"><SectionLabel>Rythme d’entraînement</SectionLabel><p className="mt-5 text-5xl font-extrabold tracking-[-.08em] text-secondary-foreground" data-testid="text-streak">{dashboard.streak}<span className="ml-2 text-base font-bold tracking-normal">jours</span></p><p className="mt-3 text-sm leading-6 text-secondary-foreground/70">La régularité compte plus qu’un score parfait. Revenez demain pour repérer un nouveau signal.</p><div className="mt-6 h-px bg-secondary-foreground/15" /><div className="mt-4 flex items-center justify-between text-xs font-bold text-secondary-foreground"><span>Progression du niveau</span><span>{score}%</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-secondary-foreground/15"><div className="h-full rounded-full bg-secondary-foreground" style={{ width: `${score}%` }} /></div></section></div></AppShell>;
}

export function CampaignsPage() {
  const { id } = useParams<{ id?: string }>();
  return id ? <CampaignDetail id={id} /> : <CampaignLibrary />;
}

function CampaignLibrary() {
  const query = useListCampaigns();
  const [filter, setFilter] = useState('Tous');
  const campaigns = query.data || [];
  const categories = ['Tous', ...Array.from(new Set(campaigns.map(c => c.category)))];
  const visible = filter === 'Tous' ? campaigns : campaigns.filter(c => c.category === filter);
  if (query.isLoading) return <AppShell><LoadingState label="Recherche de scénarios sûrs" /></AppShell>;
  if (query.isError) return <AppShell><ErrorState onRetry={() => query.refetch()} /></AppShell>;
  return <AppShell><PageHeader eyebrow="Bibliothèque de scénarios" title="Entraînez votre second regard." description="Chaque scénario est fictif, sans danger et conçu pour rendre un signal plus facile à reconnaître." action={<div className="flex items-center gap-2 rounded-xl bg-muted px-3 py-2 text-xs font-bold text-muted-foreground"><ShieldCheck size={15} className="text-primary" /> aucun vrai lien</div>} /><div className="mb-6 flex flex-wrap gap-2">{categories.map(category => <button key={category} onClick={() => setFilter(category)} className={cx('rounded-full border px-3 py-2 text-xs font-bold transition', filter === category ? 'border-primary bg-primary text-primary-foreground' : 'border-border bg-card text-muted-foreground hover:border-primary/40')} data-testid={`button-filter-${category.toLowerCase()}`}>{category}</button>)}</div>{visible.length === 0 ? <EmptyState icon={Radar} title="Aucun scénario pour le moment" message="La bibliothèque est en préparation. Revenez bientôt pour découvrir un nouveau scénario sûr." /> : <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{visible.map((campaign, index) => <Link href={`/campaigns/${campaign.id}`} key={campaign.id} className="group relative flex min-h-[245px] flex-col rounded-2xl border border-border bg-card p-6 transition duration-300 hover:-translate-y-1 hover:border-primary/50 hover:shadow-[0_16px_35px_hsl(var(--foreground)/.06)]" data-testid={`card-campaign-${campaign.id}`}><div className="flex items-start justify-between gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-primary/10 font-mono text-xs font-medium text-primary">0{index + 1}</div><Pill tone={campaign.difficulty === 'advanced' ? 'red' : campaign.difficulty === 'intermediate' ? 'amber' : 'teal'}>{difficultyLabels[campaign.difficulty ?? ''] || campaign.difficulty || 'débutant'}</Pill></div><p className="mt-8 font-mono text-[10px] uppercase tracking-[.16em] text-muted-foreground">{campaign.category}</p><h2 className="mt-2 text-lg font-extrabold tracking-[-.035em] group-hover:text-primary" data-testid={`text-campaign-title-${campaign.id}`}>{campaign.title}</h2><p className="mt-2 line-clamp-2 text-sm leading-6 text-muted-foreground">{campaign.description}</p><span className="mt-auto flex items-center gap-2 pt-5 text-xs font-bold text-primary">Ouvrir le scénario <ArrowRight size={14} className="transition-transform group-hover:translate-x-1" /></span></Link>)}</div>}</AppShell>;
}

function CampaignDetail({ id }: { id: string }) {
  const queryClient = useQueryClient();
  const query = useGetCampaign(id, { query: { enabled: !!id, queryKey: getGetCampaignQueryKey(id) } });
  const attempt = useSubmitCampaignAttempt();
  const [answer, setAnswer] = useState<'phishing' | 'legitimate' | null>(null);
  const [result, setResult] = useState<{ correct: boolean; explanation: string; correctAnswer: string } | null>(null);
  if (query.isLoading) return <AppShell><LoadingState label="Ouverture du scénario" /></AppShell>;
  if (query.isError || !query.data) return <AppShell><ErrorState onRetry={() => query.refetch()} message="Ce scénario est indisponible pour le moment." /></AppShell>;
  const campaign = query.data;
  const submit = () => { if (!answer) return; attempt.mutate({ id, data: { answer } }, { onSuccess: data => { setResult(data); queryClient.invalidateQueries({ queryKey: getGetDashboardQueryKey() }); } }); };
  return <AppShell><Link href="/campaigns" className="mb-7 inline-flex items-center gap-2 text-sm font-bold text-muted-foreground hover:text-foreground" data-testid="link-back-campaigns"><ArrowLeft size={15} /> Retour à la bibliothèque</Link><div className="grid gap-7 xl:grid-cols-[1fr_360px]"><div><div className="flex flex-wrap items-center gap-2"><Pill>{campaign.category}</Pill><Pill tone={campaign.difficulty === 'advanced' ? 'red' : 'amber'}>{difficultyLabels[campaign.difficulty ?? ''] || campaign.difficulty || 'débutant'}</Pill></div><h1 className="mt-5 max-w-3xl text-4xl font-extrabold tracking-[-.065em] sm:text-5xl" data-testid="text-campaign-detail-title">{campaign.title}</h1><p className="mt-4 max-w-2xl text-base leading-7 text-muted-foreground">{campaign.description}</p><div className="scan-line mt-8 rounded-2xl border border-border bg-card p-5 shadow-[0_14px_35px_hsl(var(--foreground)/.045)] sm:p-7"><div className="flex items-center gap-3 border-b border-border pb-5"><div className="flex h-10 w-10 items-center justify-center rounded-full bg-secondary text-xs font-extrabold text-secondary-foreground">{campaign.sender.slice(0, 2).toUpperCase()}</div><div><p className="text-sm font-bold">{campaign.sender}</p><p className="font-mono text-[10px] text-muted-foreground">expéditeur fictif</p></div></div><p className="mt-6 text-base font-extrabold">{campaign.subject}</p><p className="mt-4 whitespace-pre-line text-sm leading-7 text-muted-foreground">{campaign.content}</p><div className="mt-5 rounded-xl border border-accent/50 bg-accent/10 px-4 py-3 font-mono text-xs text-amber-900">{campaign.link}</div></div></div><aside className="h-fit rounded-2xl border border-border bg-card p-6 xl:sticky xl:top-24"><SectionLabel>À vous de décider</SectionLabel><h2 className="mt-3 text-xl font-extrabold tracking-[-.04em]">Ce message semble-t-il sûr ?</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Observez l’expéditeur, la demande et la destination avant de choisir.</p><div className="mt-6 space-y-2"><button onClick={() => setAnswer('phishing')} className={cx('flex w-full items-center gap-3 rounded-xl border p-3 text-left text-sm font-bold transition', answer === 'phishing' ? 'border-destructive bg-destructive/5 text-destructive' : 'border-border hover:border-destructive/40')} data-testid="button-answer-phishing"><ShieldAlert size={18} /> C’est du hameçonnage</button><button onClick={() => setAnswer('legitimate')} className={cx('flex w-full items-center gap-3 rounded-xl border p-3 text-left text-sm font-bold transition', answer === 'legitimate' ? 'border-primary bg-primary/5 text-primary' : 'border-border hover:border-primary/40')} data-testid="button-answer-legitimate"><ShieldCheck size={18} /> C’est légitime</button></div>{!result ? <Button className="mt-5 w-full" disabled={!answer || attempt.isPending} onClick={submit} data-testid="button-submit-campaign">{attempt.isPending ? 'Vérification…' : 'Vérifier ma réponse'} <ArrowRight size={15} /></Button> : <div className={cx('mt-5 rounded-xl p-4', result.correct ? 'bg-primary/10' : 'bg-accent/15')} data-testid="status-campaign-result"><div className="flex items-center gap-2 font-bold">{result.correct ? <Check size={17} className="text-primary" /> : <ShieldAlert size={17} className="text-amber-800" />}{result.correct ? 'Bonne lecture.' : 'Une erreur utile.'}</div><p className="mt-2 text-sm leading-6 text-muted-foreground">{result.explanation}</p><p className="mt-3 font-mono text-[10px] uppercase tracking-wider text-muted-foreground">Bonne réponse : {answerLabels[result.correctAnswer] || result.correctAnswer}</p></div>}</aside></div><section className="mt-8 rounded-2xl border border-border bg-muted/45 p-6"><SectionLabel>Banque d’indices</SectionLabel><h2 className="mt-2 text-xl font-extrabold tracking-[-.04em]">Les détails à remarquer</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{campaign.suspiciousClues.map((clue, index) => <div key={clue} className="flex gap-3 rounded-xl border border-border bg-card p-4" data-testid={`text-clue-${index}`}><span className="font-mono text-xs text-primary">0{index + 1}</span><p className="text-sm leading-6">{clue}</p></div>)}</div></section></AppShell>;
}

export function QuizPage() {
  const query = useListQuizQuestions();
  const submitQuiz = useSubmitQuiz();
  const queryClient = useQueryClient();
  const [current, setCurrent] = useState(0);
  const [answers, setAnswers] = useState<Record<string, number>>({});
  const [result, setResult] = useState<ReturnType<typeof useSubmitQuiz>['data']>(undefined);
  const questions = query.data || [];
  if (query.isLoading) return <AppShell><LoadingState label="Préparation de votre quiz" /></AppShell>;
  if (query.isError) return <AppShell><ErrorState onRetry={() => query.refetch()} /></AppShell>;
  if (!questions.length) return <AppShell><PageHeader eyebrow="Quiz rapide" title="Un quiz sans question, c’est un état vide réussi." /><EmptyState icon={FileCheck2} title="Le quiz est en préparation" message="Aucune question n’est disponible pour le moment. Essayez un scénario pendant que la prochaine série est préparée." action={<Link href="/campaigns" className="inline-flex h-10 items-center gap-2 rounded-lg bg-primary px-4 text-sm font-bold text-primary-foreground" data-testid="link-quiz-empty-campaigns">Ouvrir le laboratoire <ArrowRight size={15} /></Link>} /></AppShell>;
  const question = questions[current];
  const choose = (index: number) => setAnswers(prev => ({ ...prev, [question.id]: index }));
  const finish = () => submitQuiz.mutate({ data: { answers: questions.map(q => ({ questionId: q.id, answerIndex: answers[q.id] ?? -1 })) } }, { onSuccess: data => { setResult(data); queryClient.invalidateQueries({ queryKey: getGetDashboardQueryKey() }); queryClient.invalidateQueries({ queryKey: getListRecommendationsQueryKey() }); queryClient.invalidateQueries({ queryKey: getGetAdminStatsQueryKey() }); } });
  if (result) return <AppShell><PageHeader eyebrow="Quiz terminé" title="Vous avez fait votre choix. Utilisez maintenant le retour." description="Un score est un signal, pas un verdict. La prochaine étape est la partie la plus utile." /><div className="mx-auto max-w-2xl rounded-3xl border border-border bg-card p-7 text-center shadow-[0_16px_45px_hsl(var(--foreground)/.05)] sm:p-12"><div className="mx-auto flex h-20 w-20 items-center justify-center rounded-full bg-primary/10 text-3xl font-extrabold text-primary" data-testid="text-quiz-score">{result.score}%</div><p className="mt-5 font-mono text-[10px] uppercase tracking-[.18em] text-primary">{result.level}</p><h2 className="mt-2 text-3xl font-extrabold tracking-[-.06em]">{result.correctAnswers} signal{result.correctAnswers > 1 ? 's' : ''} correctement identifié{result.correctAnswers > 1 ? 's' : ''} sur {result.totalQuestions}</h2><p className="mx-auto mt-4 max-w-lg text-sm leading-6 text-muted-foreground" data-testid="text-quiz-recommendation">{result.recommendation}</p><div className="mt-8 flex flex-wrap justify-center gap-3"><Button onClick={() => { setResult(undefined); setCurrent(0); setAnswers({}); }} data-testid="button-retake-quiz">Recommencer</Button><Link href="/recommendations" className="inline-flex h-10 items-center gap-2 rounded-[10px] border border-border px-4 text-sm font-bold hover:border-primary/40" data-testid="link-quiz-guidance">Voir mes conseils <ArrowRight size={15} /></Link></div></div></AppShell>;
  const selected = answers[question.id];
  return <AppShell><PageHeader eyebrow={`Quiz rapide / ${String(current + 1).padStart(2, '0')} sur ${questions.length}`} title="Repérez-vous le signal&nbsp;?" description="Choisissez la meilleure réponse. Vous verrez l’ensemble du retour après la dernière question." /><div className="mx-auto max-w-3xl"><ProgressBar value={(current / questions.length) * 100} /><div className="mt-8 rounded-3xl border border-border bg-card p-6 sm:p-10"><div className="flex items-center justify-between"><Pill>{question.category}</Pill><span className="font-mono text-xs text-muted-foreground">{String(current + 1).padStart(2, '0')} / {String(questions.length).padStart(2, '0')}</span></div><h2 className="mt-8 text-2xl font-extrabold leading-tight tracking-[-.045em] sm:text-3xl" data-testid={`text-question-${question.id}`}>{question.question}</h2><div className="mt-8 space-y-3">{question.options.map((option, index) => <button key={option} onClick={() => choose(index)} className={cx('flex w-full items-start gap-4 rounded-xl border p-4 text-left text-sm font-semibold transition', selected === index ? 'border-primary bg-primary/5 text-foreground shadow-[0_0_0_3px_hsl(var(--primary)/.1)]' : 'border-border hover:border-primary/40')} data-testid={`button-option-${question.id}-${index}`}><span className={cx('flex h-7 w-7 shrink-0 items-center justify-center rounded-lg font-mono text-xs', selected === index ? 'bg-primary text-primary-foreground' : 'bg-muted text-muted-foreground')}>{String.fromCharCode(65 + index)}</span><span className="pt-1">{option}</span>{selected === index && <Check size={17} className="ml-auto mt-1 text-primary" />}</button>)}</div><div className="mt-9 flex justify-end"><Button disabled={selected === undefined || submitQuiz.isPending} onClick={() => current === questions.length - 1 ? finish() : setCurrent(prev => prev + 1)} data-testid="button-next-question">{submitQuiz.isPending ? 'Calcul…' : current === questions.length - 1 ? 'Terminer le quiz' : 'Question suivante'} <ArrowRight size={15} /></Button></div></div></div></AppShell>;
}

export function RecommendationsPage() {
  const query = useListRecommendations();
  if (query.isLoading) return <AppShell><LoadingState label="Personnalisation de vos conseils" /></AppShell>;
  if (query.isError) return <AppShell><ErrorState onRetry={() => query.refetch()} /></AppShell>;
  const recommendations = query.data || [];
  return <AppShell><PageHeader eyebrow="Conseils personnalisés" title="Quelques points à garder en tête." description="Vos conseils restent ciblés&nbsp;: une habitude, une raison et une prochaine étape utile." />{recommendations.length === 0 ? <EmptyState icon={ShieldCheck} title="Vous êtes à jour" message="Aucun nouveau conseil pour le moment. Continuez à vous entraîner et nous vous proposerons le prochain axe utile." /> : <div className="grid gap-4 md:grid-cols-2">{recommendations.map(rec => <article key={rec.id} className="rounded-2xl border border-border bg-card p-6" data-testid={`card-recommendation-${rec.id}`}><div className="flex items-center justify-between"><Pill tone={rec.priority === 'high' ? 'red' : rec.priority === 'medium' ? 'amber' : 'teal'}>{priorityLabels[rec.priority] || rec.priority}</Pill><span className="font-mono text-[10px] text-muted-foreground">{rec.focus}</span></div><h2 className="mt-6 text-xl font-extrabold tracking-[-.04em]" data-testid={`text-recommendation-title-${rec.id}`}>{rec.title}</h2><p className="mt-3 text-sm leading-7 text-muted-foreground">{rec.message}</p><div className="mt-6 flex items-center gap-2 text-xs font-bold text-primary"><span className="h-2 w-2 rounded-full bg-primary" /> Axe conseillé&nbsp;: {rec.focus}</div></article>)}</div>}</AppShell>;
}

export function ResultsPage() {
  const dashboard = useGetDashboard();
  if (dashboard.isLoading) return <AppShell><LoadingState label="Récupération de votre historique" /></AppShell>;
  if (dashboard.isError || !dashboard.data) return <AppShell><ErrorState onRetry={() => dashboard.refetch()} /></AppShell>;
  const d = dashboard.data;
  return <AppShell><PageHeader eyebrow="Mes résultats" title="Votre progression, simplement." description="Votre historique s’enrichira à chaque quiz terminé. Pour l’instant, voici votre dernier signal." /><div className="grid gap-4 sm:grid-cols-3"><StatCard label="Dernier score" value={`${d.latestScore}%`} detail="résultat du quiz le plus récent" icon={Target} /><StatCard label="Quiz réalisés" value={d.quizzesCompleted} detail="total envoyé" icon={FileCheck2} tone="amber" /><StatCard label="Niveau" value={d.level || 'À découvrir'} detail={`${d.streak} jour${d.streak > 1 ? 's' : ''} d’affilée`} icon={Sparkles} tone="slate" /></div><div className="mt-6 rounded-2xl border border-dashed border-border bg-card p-8" data-testid="status-results-history"><SectionLabel>Historique des scores</SectionLabel><h2 className="mt-3 text-xl font-extrabold tracking-[-.04em]">{d.quizzesCompleted > 0 ? 'Votre historique se construit.' : 'Aucun score de quiz pour le moment.'}</h2><p className="mt-2 max-w-xl text-sm leading-6 text-muted-foreground">{d.quizzesCompleted > 0 ? 'Terminez un autre quiz pour rendre la tendance plus utile. Cette page rassemblera vos résultats au fil de votre entraînement.' : 'Faites le quiz rapide quand vous êtes prêt. Il est court, fictif et conçu pour vous apprendre, pas pour vous juger.'}</p><Link href="/quiz" className="mt-5 inline-flex h-10 items-center gap-2 rounded-lg bg-primary px-4 text-sm font-bold text-primary-foreground" data-testid="link-results-quiz">Faire un quiz <ArrowRight size={15} /></Link></div></AppShell>;
}

export function SettingsPage() {
  const { user, isLoaded } = useUser();
  const { signOut } = useClerk();
  if (!isLoaded) return <AppShell><LoadingState label="Chargement de votre profil" /></AppShell>;
  return <AppShell><PageHeader eyebrow="Paramètres du compte" title="Gardez votre compte à votre image." description="Votre espace d’apprentissage vous est réservé. Consultez ici votre profil et les conseils de session." /><div className="grid gap-6 lg:grid-cols-[1fr_360px]"><section className="rounded-2xl border border-border bg-card p-6 sm:p-8"><SectionLabel>Profil</SectionLabel><div className="mt-6 flex items-center gap-4"><div className="flex h-14 w-14 items-center justify-center rounded-2xl bg-secondary text-lg font-extrabold text-secondary-foreground">{(user?.firstName?.[0] || user?.emailAddresses[0]?.emailAddress?.[0] || 'A').toUpperCase()}</div><div><h2 className="text-lg font-extrabold">{user?.fullName || 'Apprenant'}</h2><p className="text-sm text-muted-foreground">{user?.emailAddresses[0]?.emailAddress}</p></div></div><div className="mt-8 grid gap-4 sm:grid-cols-2"><div className="rounded-xl bg-muted/60 p-4"><p className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Prénom</p><p className="mt-2 text-sm font-semibold">{user?.firstName || 'Non renseigné'}</p></div><div className="rounded-xl bg-muted/60 p-4"><p className="text-xs font-bold uppercase tracking-wider text-muted-foreground">E-mail</p><p className="mt-2 truncate text-sm font-semibold">{user?.emailAddresses[0]?.emailAddress || 'Non renseigné'}</p></div></div></section><section className="h-fit rounded-2xl border border-border bg-card p-6"><SectionLabel>Conseils de session</SectionLabel><h2 className="mt-4 text-xl font-extrabold tracking-[-.04em]">Utilisez un appareil privé si possible.</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">PhishGuard utilise votre session Clerk. Déconnectez-vous des ordinateurs partagés et ne saisissez jamais de vrai mot de passe dans un scénario d’entraînement.</p><Button variant="secondary" className="mt-6 w-full" onClick={() => signOut({ redirectUrl: import.meta.env.BASE_URL || '/' })} data-testid="button-settings-signout"><LogOut size={16} /> Se déconnecter</Button></section></div></AppShell>;
}

function AdminDenied() { return <AppShell><div className="mx-auto max-w-xl py-14 text-center"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-2xl bg-accent/20 text-amber-800"><LockKeyhole size={27} /></div><h1 className="mt-6 text-3xl font-extrabold tracking-[-.06em]">Accès administrateur limité.</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">Cette console est réservée aux administrateurs PhishGuard autorisés. Votre compte apprenant reste entièrement disponible.</p><Link href="/dashboard" className="mt-6 inline-flex h-10 items-center gap-2 rounded-lg bg-primary px-4 text-sm font-bold text-primary-foreground" data-testid="link-admin-denied-dashboard">Retour à la vue d’ensemble <ArrowRight size={15} /></Link></div></AppShell>; }

export function AdminPage() {
  const { user, isLoaded } = useUser();
  const allowed = (user?.publicMetadata as { role?: string } | undefined)?.role === 'admin';
  if (!isLoaded) return <AppShell><LoadingState label="Vérification des droits administrateur" /></AppShell>;
  if (!allowed) return <AdminDenied />;
  return <AdminConsole />;
}

function AdminConsole() {
  const stats = useGetAdminStats();
  const campaigns = useListCampaigns();
  const questions = useListQuizQuestions();
  const queryClient = useQueryClient();
  const [tab, setTab] = useState<'overview' | 'campaigns' | 'quiz'>('overview');
  const [campaignForm, setCampaignForm] = useState<Campaign | null>(null);
  const [questionForm, setQuestionForm] = useState<QuizQuestion | null>(null);
  const [newCampaign, setNewCampaign] = useState(false);
  const [newQuestion, setNewQuestion] = useState(false);
  const createCampaign = useCreateCampaign();
  const updateCampaign = useUpdateCampaign();
  const deleteCampaign = useDeleteCampaign();
  const createQuestion = useCreateQuizQuestion();
  const updateQuestion = useUpdateQuizQuestion();
  const deleteQuestion = useDeleteQuizQuestion();
  const refreshCampaigns = () => { queryClient.invalidateQueries({ queryKey: getListCampaignsQueryKey() }); queryClient.invalidateQueries({ queryKey: getGetAdminStatsQueryKey() }); };
  const refreshQuestions = () => { queryClient.invalidateQueries({ queryKey: getListQuizQuestionsQueryKey() }); queryClient.invalidateQueries({ queryKey: getGetAdminStatsQueryKey() }); };
  if (stats.isLoading || campaigns.isLoading || questions.isLoading) return <AppShell><LoadingState label="Chargement de l’espace d’administration" /></AppShell>;
  if (stats.isError || campaigns.isError || questions.isError || !stats.data) return <AppShell><ErrorState onRetry={() => { stats.refetch(); campaigns.refetch(); questions.refetch(); }} /></AppShell>;
  const s = stats.data;
  const saveCampaign = (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); const fd = new FormData(event.currentTarget); const data = { title: String(fd.get('title')), description: String(fd.get('description')), sender: String(fd.get('sender')), subject: String(fd.get('subject')), content: String(fd.get('content')), link: String(fd.get('link')), suspiciousClues: String(fd.get('suspiciousClues')).split('\n').filter(Boolean), correctAnswer: String(fd.get('correctAnswer')) as 'phishing' | 'legitimate', explanation: String(fd.get('explanation')), category: String(fd.get('category')), difficulty: String(fd.get('difficulty')) as 'beginner' | 'intermediate' | 'advanced' }; const done = () => { refreshCampaigns(); setCampaignForm(null); setNewCampaign(false); }; if (campaignForm) updateCampaign.mutate({ id: campaignForm.id, data }, { onSuccess: done }); else createCampaign.mutate({ data }, { onSuccess: done }); };
  const saveQuestion = (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); const fd = new FormData(event.currentTarget); const data = { question: String(fd.get('question')), options: String(fd.get('options')).split('\n').filter(Boolean), correctAnswer: Number(fd.get('correctAnswer')), category: String(fd.get('category')), explanation: String(fd.get('explanation')) }; const done = () => { refreshQuestions(); setQuestionForm(null); setNewQuestion(false); }; if (questionForm) updateQuestion.mutate({ id: questionForm.id, data }, { onSuccess: done }); else createQuestion.mutate({ data }, { onSuccess: done }); };
  return <AppShell><PageHeader eyebrow="Console d’administration" title="Gardez l’espace d’entraînement pertinent." description="Gérez les campagnes fictives, la qualité des questions et les indicateurs d’apprentissage anonymisés." action={<Pill tone="amber"><Users size={13} /> accès équipe</Pill>} /><div className="mb-7 flex gap-1 rounded-xl bg-muted p-1 sm:w-fit">{(['overview', 'campaigns', 'quiz'] as const).map(item => <button key={item} onClick={() => setTab(item)} className={cx('rounded-lg px-4 py-2 text-xs font-bold transition', tab === item ? 'bg-card text-foreground shadow-sm' : 'text-muted-foreground hover:text-foreground')} data-testid={`button-admin-tab-${item}`}>{item === 'overview' ? 'Vue d’ensemble' : item === 'campaigns' ? 'Campagnes' : 'Questions du quiz'}</button>)}</div>{tab === 'overview' && <div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-5"><StatCard label="Utilisateurs" value={s.users} icon={Users} /><StatCard label="Campagnes" value={s.campaigns} icon={Radar} tone="amber" /><StatCard label="Quiz réalisés" value={s.quizzesCompleted} icon={FileCheck2} /><StatCard label="Score moyen" value={`${s.averageScore}%`} icon={Target} tone="slate" /><StatCard label="Taux de réussite" value={`${s.successRate}%`} icon={ShieldCheck} /></div><div className="mt-6 grid gap-6 lg:grid-cols-[1fr_1fr]"><section className="rounded-2xl border border-border bg-card p-6"><SectionLabel>Répartition des niveaux</SectionLabel><div className="mt-6 space-y-4">{s.levels.map(level => <div key={level.label}><div className="flex justify-between text-xs font-bold"><span>{level.label}</span><span>{level.value}</span></div><div className="mt-2 h-2 rounded-full bg-muted"><div className="h-full rounded-full bg-primary" style={{ width: `${Math.min(100, level.value)}%` }} /></div></div>)}</div></section><section className="rounded-2xl border border-border bg-sidebar p-6 text-sidebar-foreground"><SectionLabel><span className="text-sidebar-primary">État des accès</span></SectionLabel><h2 className="mt-4 text-2xl font-extrabold tracking-[-.05em]">Anonymisé par défaut.</h2><p className="mt-3 text-sm leading-6 text-sidebar-foreground/65">Utilisez ces indicateurs globaux pour ajuster le programme sans transformer les apprenants en classement.</p><div className="mt-7 grid grid-cols-2 gap-3"><div className="rounded-xl bg-sidebar-accent p-4"><p className="font-mono text-[10px] uppercase text-sidebar-foreground/50">Données apprenants</p><p className="mt-2 text-sm font-bold">Privées</p></div><div className="rounded-xl bg-sidebar-accent p-4"><p className="font-mono text-[10px] uppercase text-sidebar-foreground/50">Statistiques</p><p className="mt-2 text-sm font-bold">Agrégées</p></div></div></section></div></div>}{tab === 'campaigns' && <AdminCampaigns campaigns={campaigns.data || []} onNew={() => setNewCampaign(true)} onEdit={setCampaignForm} onDelete={id => { if (window.confirm('Supprimer cette campagne fictive&nbsp;?')) deleteCampaign.mutate({ id }, { onSuccess: refreshCampaigns }); }} />} {tab === 'quiz' && <AdminQuestions questions={questions.data || []} onNew={() => setNewQuestion(true)} onEdit={setQuestionForm} onDelete={id => { if (window.confirm('Supprimer cette question du quiz&nbsp;?')) deleteQuestion.mutate({ id }, { onSuccess: refreshQuestions }); }} />} {(newCampaign || campaignForm) && <CampaignForm initial={campaignForm} onClose={() => { setCampaignForm(null); setNewCampaign(false); }} onSubmit={saveCampaign} pending={createCampaign.isPending || updateCampaign.isPending} />} {(newQuestion || questionForm) && <QuestionForm initial={questionForm} onClose={() => { setQuestionForm(null); setNewQuestion(false); }} onSubmit={saveQuestion} pending={createQuestion.isPending || updateQuestion.isPending} />}</AppShell>;
}

function AdminCampaigns({ campaigns, onNew, onEdit, onDelete }: { campaigns: Campaign[]; onNew: () => void; onEdit: (c: Campaign) => void; onDelete: (id: string) => void }) { return <section className="rounded-2xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border p-5 sm:p-6"><div><SectionLabel>Inventaire des contenus</SectionLabel><h2 className="mt-2 text-xl font-extrabold tracking-[-.04em]">Campagnes</h2></div><Button onClick={onNew} data-testid="button-new-campaign"><Plus size={16} /> Nouvelle campagne</Button></div>{campaigns.length === 0 ? <div className="p-6"><EmptyState title="Aucune campagne" message="Créez le premier scénario d’apprentissage sûr." /></div> : <div className="divide-y divide-border">{campaigns.map(c => <div className="flex items-center justify-between gap-4 p-5" key={c.id} data-testid={`row-admin-campaign-${c.id}`}><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><p className="truncate text-sm font-bold">{c.title}</p><Pill tone="slate">{c.category}</Pill></div><p className="mt-1 truncate text-xs text-muted-foreground">{c.description}</p></div><div className="flex shrink-0 gap-1"><Button variant="ghost" className="px-2.5" onClick={() => onEdit(c)} aria-label={`Modifier ${c.title}`} data-testid={`button-edit-campaign-${c.id}`}><Pencil size={15} /></Button><Button variant="ghost" className="px-2.5 text-destructive hover:bg-destructive/10" onClick={() => onDelete(c.id)} aria-label={`Supprimer ${c.title}`} data-testid={`button-delete-campaign-${c.id}`}><Trash2 size={15} /></Button></div></div>)}</div>}</section>; }

function AdminQuestions({ questions, onNew, onEdit, onDelete }: { questions: QuizQuestion[]; onNew: () => void; onEdit: (q: QuizQuestion) => void; onDelete: (id: string) => void }) { return <section className="rounded-2xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border p-5 sm:p-6"><div><SectionLabel>Banque de questions</SectionLabel><h2 className="mt-2 text-xl font-extrabold tracking-[-.04em]">Questions du quiz</h2></div><Button onClick={onNew} data-testid="button-new-question"><Plus size={16} /> Nouvelle question</Button></div>{questions.length === 0 ? <div className="p-6"><EmptyState title="Aucune question" message="Créez la première question du quiz." /></div> : <div className="divide-y divide-border">{questions.map(q => <div className="flex items-center justify-between gap-4 p-5" key={q.id} data-testid={`row-admin-question-${q.id}`}><div className="min-w-0"><p className="line-clamp-2 text-sm font-bold">{q.question}</p><p className="mt-1 font-mono text-[10px] uppercase tracking-wider text-muted-foreground">{q.category} · {q.options.length} options</p></div><div className="flex shrink-0 gap-1"><Button variant="ghost" className="px-2.5" onClick={() => onEdit(q)} aria-label="Modifier la question" data-testid={`button-edit-question-${q.id}`}><Pencil size={15} /></Button><Button variant="ghost" className="px-2.5 text-destructive hover:bg-destructive/10" onClick={() => onDelete(q.id)} aria-label="Supprimer la question" data-testid={`button-delete-question-${q.id}`}><Trash2 size={15} /></Button></div></div>)}</div>}</section>; }

function CampaignForm({ initial, onClose, onSubmit, pending }: { initial: Campaign | null; onClose: () => void; onSubmit: (event: React.FormEvent<HTMLFormElement>) => void; pending: boolean }) { const c = initial; return <Modal title={c ? 'Modifier la campagne' : 'Nouvelle campagne'} onClose={onClose}><form onSubmit={onSubmit} className="space-y-4"><div className="grid gap-4 sm:grid-cols-2"><Field name="title" label="Titre" defaultValue={c?.title} required /><Field name="category" label="Catégorie" defaultValue={c?.category} required /><Field name="sender" label="Expéditeur" defaultValue={c?.sender} required /><Field name="subject" label="Objet" defaultValue={c?.subject} required /></div><TextAreaField name="description" label="Description" defaultValue={c?.description} required /><TextAreaField name="content" label="Contenu du message" defaultValue={c?.content} required /><Field name="link" label="Lien affiché dans le message" defaultValue={c?.link} required /><TextAreaField name="suspiciousClues" label="Indices suspects (un par ligne)" defaultValue={c?.suspiciousClues.join('\n')} required /><TextAreaField name="explanation" label="Explication" defaultValue={c?.explanation} required /><div className="grid gap-4 sm:grid-cols-2"><label className="space-y-2"><span className="text-xs font-bold uppercase tracking-[.08em] text-muted-foreground">Bonne réponse</span><select name="correctAnswer" defaultValue={c?.correctAnswer || 'phishing'} className="h-11 w-full rounded-xl border border-input bg-background px-3 text-sm"><option value="phishing">Hameçonnage</option><option value="legitimate">Légitime</option></select></label><label className="space-y-2"><span className="text-xs font-bold uppercase tracking-[.08em] text-muted-foreground">Difficulté</span><select name="difficulty" defaultValue={c?.difficulty || 'beginner'} className="h-11 w-full rounded-xl border border-input bg-background px-3 text-sm"><option value="beginner">Débutant</option><option value="intermediate">Intermédiaire</option><option value="advanced">Avancé</option></select></label></div><div className="flex justify-end gap-2 pt-2"><Button type="button" variant="ghost" onClick={onClose} data-testid="button-cancel-campaign">Annuler</Button><Button type="submit" disabled={pending} data-testid="button-save-campaign"><Save size={15} /> {pending ? 'Enregistrement…' : 'Enregistrer la campagne'}</Button></div></form></Modal>; }

function QuestionForm({ initial, onClose, onSubmit, pending }: { initial: QuizQuestion | null; onClose: () => void; onSubmit: (event: React.FormEvent<HTMLFormElement>) => void; pending: boolean }) { const q = initial; return <Modal title={q ? 'Modifier la question' : 'Nouvelle question'} onClose={onClose}><form onSubmit={onSubmit} className="space-y-4"><TextAreaField name="question" label="Question" defaultValue={q?.question} required /><TextAreaField name="options" label="Réponses (une par ligne)" defaultValue={q?.options.join('\n')} required /><div className="grid gap-4 sm:grid-cols-2"><Field name="correctAnswer" label="Numéro de la bonne réponse (à partir de 0)" type="number" min="0" max="4" defaultValue={0} required /><Field name="category" label="Catégorie" defaultValue={q?.category} required /></div><TextAreaField name="explanation" label="Explication" defaultValue={q?.explanation} required /><div className="flex justify-end gap-2 pt-2"><Button type="button" variant="ghost" onClick={onClose} data-testid="button-cancel-question">Annuler</Button><Button type="submit" disabled={pending} data-testid="button-save-question"><Save size={15} /> {pending ? 'Enregistrement…' : 'Enregistrer la question'}</Button></div></form></Modal>; }

function Modal({ title, onClose, children }: { title: string; onClose: () => void; children: React.ReactNode }) { return <div className="fixed inset-0 z-50 flex items-center justify-center bg-foreground/35 p-4" role="dialog" aria-modal="true" data-testid="dialog-admin-form"><div className="max-h-[90dvh] w-full max-w-2xl overflow-y-auto rounded-2xl border border-border bg-card p-6 shadow-2xl sm:p-8"><div className="flex items-center justify-between"><h2 className="text-xl font-extrabold tracking-[-.04em]">{title}</h2><button onClick={onClose} className="rounded-lg p-2 text-muted-foreground hover:bg-muted hover:text-foreground" aria-label="Fermer le formulaire" data-testid="button-close-form">×</button></div><div className="mt-6">{children}</div></div></div>; }

export { DashboardPage };